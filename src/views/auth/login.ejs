<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body style="display: flex; flex-direction: column;">
    <h1 style="align-self: center;">Login</h1>
    <!-- Error box -->
    <% if (locals.error) { %> 
        <%- include('../error.ejs', error); %>
    <% };%>
    <form action="/auth/login" method="POST" style="display: flex; flex-direction: column; align-self: center;">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label for="login">Login</label>
        <input type="text" name ="login" required value="admin@admin.com">
        <label for="password">Password</label>
        <input type="text" name ="password" required value="admin123456">
        <br>
        <input type="submit">
    </form>
    <br>
    <a href="/auth/register"  style="align-self: center;">Register</a>
</body>
</html>